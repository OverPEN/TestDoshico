<Window x:Class="TestDoshico.QuesitiMainFrame"
        x:Name="QuesitiMainFrameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModels="clr-namespace:TestDoshico.ViewModels"
        mc:Ignorable="d"
        Title="Test Doshico" Height="600" Width="800">
    <Window.DataContext>
        <ViewModels:QuesitiMainFrameViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="PRAKRITI TEST"/>
        <Label Grid.Row="1" Content="COSTITUZIONE INDIVIDUALE AYURVEDICA"/>
        <Button Grid.Row="2" Content="Torna al Menù Principale" HorizontalAlignment="Center" VerticalAlignment="Center" Command="{Binding MenuPrincipaleCommand}" CommandParameter="{Binding ElementName=QuesitiMainFrameWindow}"/>
        <Frame Grid.Row="3" Name="Main" NavigationUIVisibility="Visible" Style="{DynamicResource FrameNewNavigationUI}" Height="Auto" Width="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
